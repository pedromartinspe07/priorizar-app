/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './priorizarapp/App';
import {name as appName} from './priorizarapp/app.json';

AppRegistry.registerComponent(appName, () => App);
